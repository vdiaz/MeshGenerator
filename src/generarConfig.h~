#ifndef GENERARTGS_H
#define GENERARTGS_H

#include "comandoMTG.h"

class GenerarConfig : public ComandoMTG {

	public:
		GenerarConfig(std::map<std::string, bool> formatos_malla);
		void execute();
		~GenerarConfig() {}

};

#endif
